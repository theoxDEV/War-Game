//Establishing a connection with the server on port 5500y
const socket = io('http://localhost:3000');

socket.on('matheux', () => {
    
    //console.log("countryNextColor1", game);
    console.log("countryNextColor1");
})